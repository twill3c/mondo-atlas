node_modules/
.next/
out/
.vercel/
__pycache__/
.pytest_cache/
*.tsbuildinfo

# 生成物。raw/perseus と data/curated から `python etl/normalize.py` で再生成できる
data/works/
# data/index.json は図の材料(20KB)。Node だけでビルドできるようにコミットする
data/derived/

# 検品の画面写真(見たら捨てる)
shot-*.png

# 4 行目の .vercel/ と重複するが、vercel CLI が初回リンク時に自分で書き足したもの。
# 消すと次のデプロイでまた足されるので残しておく。
.vercel

# 交替の材料(657KB)。python etl/build_turns.py で再生成できる。
# 画面②を作るループでコミットに切り替える。
data/turns.json
# data/breath.json は画面②の材料(106KB)。ビルド時 import するのでコミットする
# data/words.json は画面③の材料(108KB)。ビルド時 import するのでコミットする
# data/style.json は画面④の材料(8KB)。ビルド時 import するのでコミットする
# data/japanese.json は画面⑤の材料(40KB)。ビルド時 import するのでコミットする
