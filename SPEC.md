# SPEC.md — mondo-atlas(問答アトラス)

<!-- scaffold template v1.24.0 から展開(2026-09-02)。以後このファイルはプロジェクトが育てる -->

## 1. 目的

Perseus Digital Library のプラトン全集(Burnet, *Platonis Opera*)を、
**ステファヌス番号という一本の背骨**で通して可視化する。

プラトンの対話篇は「対話」と呼ばれるが、対話の濃さは篇によって桁違いに違う。
同じ物差しを全 36 篇に当てて、**どの篇のどこで問答が起き、どこで独白に変わるか**を
一枚の図で見せる。あわせて希語・英訳・和訳の三段リーダーで、図の任意の点から本文へ降りられるようにする。

奇をてらった指標は置かない。数えれば出る量だけを、位置つきで見せる。

## 2. データ源

| 項目 | 値 | 出所 |
|---|---|---|
| リポジトリ | `PerseusDL/canonical-greekLit` | GitHub |
| 原文 | `tlg0059.tlg001`–`tlg036`(希) | Burnet, *Platonis Opera*, OCT, Oxford 1900–1907 |
| 英訳 | `perseus-eng2`(全 36 篇) | Fowler / Lamb ほか, Loeb Classical Library, 1914– |
| ライセンス | **CC BY-SA 4.0** | リポジトリ README |

### 実測値(2026-09-02、全 72 ファイル走査 + 正規化後の集計)

| 項目 | 実測 |
|---|---|
| 作品数 | 36 篇(希 36 / 英 36、欠けなし) |
| ギリシャ語**本文**語数 | **559,618 語**(話者記号と見出しを除く) |
| 話者記号(`<label>` の `ΣΩ.` 等) | 12,544 件(本文には含めない) |
| 英訳語数 | 778,231 語(**訳者注 4,394 件 113,710 語を本文から外した後**。外した量は index.json に載せる) |
| ステファヌス節(`17a` 形式) | **8,559 節** |
| ステファヌス頁 | 1,752 頁(節 ID から導いた作品内の異なり頁の合計) |
| `<said who>` を持つ篇 | 33 / 36 |
| 話者 ID の異なり | 57 種 |
| 最長 / 最短 | 『法律』102,743 語 / 『クリトフォン』1,509 語 |

位置の単位は全篇に `<milestone unit="section" resp="Stephanus" n="17a"/>` として入っている。
**全作品の全箇所に世界共通の安定した住所がある**ことを確認済み(2026-09-02)。

#### 数え方についての注記

- 当初 574,506 語と記録したが、これは `<label>`(話者記号)と見出しを含んでいた。
  **本文だけの語数は 559,618 語**である。
- 当初 8,560 節と記録したが、`unit="section"` の milestone のうち 1 件は
  `n="imbedded dialogue"`(テアイテトス)で、節ではない。**節は 8,559**。
- 頁数は「節 ID から導いた作品内の異なり頁の合計」で 1,752。
  `unit="page"` の milestone を数えると 1,751 で 1 件ずれる。
  **突合済み(2026-09-02)**: 差は『法律』の 1 件で、**743 頁に節 milestone はあるが
  `unit="page"` の milestone が無い**(上流の付け忘れ)。節から導いた 1,752 を正とする。

#### 正規化で判明した上流の性質

| 事象 | 件数 | 扱い |
|---|---|---|
| 英訳側に対応する節 milestone が無い | 4 節(饗宴181b・プロタゴラス332c・ヒッピアス大302d・イオン539d) | 欠落として記録。英訳文が失われたのではなく、その粒度で位置指定できないだけ |
| 節境界の前後に空白が無い | 7 件 | いずれも語中ではなく空白の省略。`glue_prev` で表現し、連結時に空白を入れない |
| 『書簡集』で節 ID が重複 | 7 件 | 13 通の切れ目が節の内側に落ちる。節 ID に手紙番号を含める(`Ep.2.310b`) |
| 上流の誤植(語末 `μεν` → `ΜΕΝ.`) | 5 件(メノン) | `data/curated/corrections.json` の補正表で派生データにのみ適用。原本は据え置き |

### 実測で壊れた前提(2026-09-02)

当初「話者別の発話量を全 36 篇で比較する」を目玉に置いた。**この前提は実測で壊れた。**

- 『国家』は 88,340 語ありながら `<said>` が 278 個・話者 **1 名**しかない
- 『饗宴』3.2、『パイドン』4.1、『カルミデス』2.9(千語あたり `<said>` 数)に対し、
  『アルキビアデスI』81.2、『パルメニデス』70.2、『ソピステス』68.3

「語り直し型(誰かが回想して語る)と上演型(直接対話)の違いだ」という説明は魅力的だが、
**反例が出る** —— 『リュシス』36.9・『プロタゴラス』18.0・『エウテュデモス』48.5 は
いずれも語り直し型でありながら密度が高い。したがってこの数字は文学形式ではなく、
**Perseus の篇ごとの校訂方針の違いを主に映している**(実測、2026-09-02)。

この結論から、F-05 の設計を次のように決めた:

> **話者の同定を Perseus のマークアップに依存させない。** 発話交替を自前で検出し、
> マークアップが濃い篇を答え合わせに使う(G-03)。画面には常に
> 「この篇の話者同定は自前推定か原データか」を出す(F-05c)。

## 3. 機能要求

| ID | 要求 | 優先度 |
|---|---|---|
| F-01 | 希語 36 篇をステファヌス節単位の JSON に正規化する。節 ID は `Resp.327a` 形式で全集一意 | must |
| F-02 | 英訳 36 篇を同じ節 ID に対応づける。対応しない節は欠落として明示する | must |
| F-03 | 作品メタデータ(希題名・和題名・略号・語数・節数・頁数・真贋区分)を持つ | must |
| F-04 | 画面①全集の俯瞰。36 篇を頁数比の帯で並べ、真贋区分で塗り分ける | must |
| F-05 | 画面②問答の呼吸。1 篇をステファヌス番号順に展開し、発話交替の密度を示す | must |
| F-05a | 発話交替を本文から自前で検出する(Perseus マークアップに依存しない) | must |
| F-05b | 篇間を同じ物差しで比較できる俯瞰(全 36 篇の密度プロファイル一覧) | must |
| F-05c | 各篇の話者同定が「原データ由来」か「自前推定」かを画面に明示する | must |
| F-06 | 画面③語の地層。主要語のヒートマップ(縦=作品、横=作品内位置) | should |
| F-07 | 画面④文体の指紋。ヒアトゥス率・応答定型句・小辞比の散布図 | should |
| F-08 | 画面⑥三段リーダー(希 / 英 / 和)。節単位で行送りを揃え、**めくる単位もステファヌス頁**にする | must |
| F-09 | 任意の節への直リンク(`?loc=Resp.327a`) | must |
| F-10 | 和訳・第1層: 36 篇の解題(各 400 字程度) | must |
| F-11 | 和訳・第2層: ステファヌス 1 頁ごとの一行要旨(1,752 本) | must |

### F-11 の進み具合(2026-09-04 実測)

**篇ごとに全頁で入れる。** 半端に埋めると充填率だけが上がり、
「この篇は読める」と誤読される(T-612 が対照つきで見ている)。

| 篇 | 頁 | 入れたループ |
|---|---|---|
| エウテュプロン / 弁明 / クリトン | 15 / 26 / 12 | L6 |
| メノン | 31 | L11 |
| 饗宴 | 52 | L12 |
| **計** | **136 / 1,752(7.8%)** | |
| F-12 | 和訳・第3層: 完訳。初回対象は「ソクラテスの裁判」三篇(17,988 語) | should |

### F-12 の対象と順序

語数は正規化後の本文語(2026-09-02 実測)。

| 順 | 篇 | 希語数 | 節数 | 状態 |
|---|---|---|---|---|
| 1 | エウテュプロン | 5,161 | 70 | **完訳(70/70 節・2026-09-03)** |
| 2 | ソクラテスの弁明 | 8,659 | 125 | **完訳(125/125 節・2026-09-03)** |
| 3 | クリトン | 4,168 | 59 | **完訳(59/59 節・2026-09-02)** |
| 4 | メノン | 9,684 | — | 未着手(次期) |
| 5 | 饗宴 | 17,304 | — | 未着手(次期) |

### F-12 の進み具合(2026-09-02 実測)

**分母は全集で取る。** 三篇だけを分母にすると進み具合が誇張される。

| 分母 | 訳済 | 割合 |
|---|---|---|
| リーダーの三篇 | **254 / 254 節** | **100%** |
| 全集 | 254 / 8,559 節 | 2.97% |

**F-12 の初回対象(ソクラテスの裁判 三篇)は完了した。**

### リーダーは篇ごとに分けてある(N-03・2026-09-03)

はじめは三篇を一枚に埋め込んでおり、配信 153KB(圧縮前 544KB)まで育っていた。
**篇を足すたびに全体が重くなる作り**だったので、篇ごとの頁に分けた。

| 道筋 | 圧縮前 | 配信 |
|---|---|---|
| `/read/`(目次・本文を持たない) | 17 KB | 5 KB |
| `/read/euthphr/` | 166 KB | 51 KB |
| `/read/ap/`(いちばん重い篇) | 279 KB | 91 KB |
| `/read/crit/` | 144 KB | 46 KB |

**篇を足しても、どの頁も重くならない。**
足すには三つが要る —— `INCLUDE` / `data/reader/<略号>.json` / `app/read/<slug>/page.tsx`。
どれかを忘れれば T-721 が落ちる。

`?loc=Crit.43a` は**既に配ってしまった住所**なので、
目次がこれを受けて篇の頁へ送る(形の検査を通ったものだけ)。

短い篇から積んだのは、**完訳を一本作らないと G-06 が空回りする**からである。
未訳節を数える仕組みは、未訳が全部の状態でも緑になってしまう。

## 4. 非機能要求

| ID | 要求 | 検証方法 |
|---|---|---|
| N-01 | Vercel 静的出力。**cron ゼロ・関数ゼロ**(底本は改訂されないので更新経路を持たない) | `next build` の出力に関数が無いこと |
| N-02 | ビルドはネットワーク非依存。取得済み TEI **と書体**をリポジトリに固定し、以後上流の改訂に揺らされない | オフラインで `next build` が通ること + T-714(`next/font/google` を使っていない) |
| N-03 | 作品ごとにデータを分割し、1 篇単位で読み込む | 初回転送量の実測 |
| N-04 | 出典表示: 各画面に底本(Burnet OCT / Loeb)と Perseus への帰属を出す | 目視検品 |

## 5. 品質ゲート

| ID | ゲート | 判定 |
|---|---|---|
| G-01 | 節 ID が全集で一意、かつ篇内でステファヌス順に単調増加する | 8,560 節すべてで成立 |
| G-02 | 字種検査。希語本文はギリシャ文字・記号・空白のみ。**キリル文字混入 0**。NFC 正規化済み | 違反 0 件 + 陽性対照が撃つこと |
| G-03 | **循環の禁止。** 自前の発話交替検出器(F-05a)を、Perseus マークアップが濃い篇で検証する。検出器は `<said>` を一切参照しない | 検出器の入力に `said` が現れないことをコードで検査 |
| G-04 | 語数保存。XML → JSON で本文語が落ちていない | 全 36 篇で希語数が実測値と一致 |
| G-05 | 希英の節対応。対応しない節は「欠落」として数え、黙って捨てない | 欠落数を明示的に記録 |
| G-06 | 和訳の脱落検査。**原文の側から**節ごとに突き合わせ、訳された篇に未訳節が無い | 完訳篇で未訳節 0 |
| G-07 | 図に添えるラベル・軸・凡例は、図を生成したのと同じデータから導く | 決め打ち座標が無いこと |
| G-08 | 複数画面幅での目視検品(横溢れ・縦伸びすぎの代理指標つき) | 検品手順の実行記録 |
| G-11 | 節ごとの交替数が、**語数という交絡**と**文末素通し**の両方の対照に勝つ | 下記の実測 |
| G-12 | 語り直し型で、人称の交替が置換対照より有意 | 下記の実測 |
| G-13 | 語形の採否台帳が**両方向**で検算されている(拾いすぎ・取りこぼし) | `etl/audit_words.py` が exit 0 |
| G-14 | 文体の主張は**測る前に予測を宣言**し、外れも書き換えずに残す | ループログの `loop_start` に記録 |
| G-15 | 節への直リンクは**壊れた住所**で既定の頁に落ちる(URL の文字列を要素の探索にそのまま渡さない) | 実ブラウザで正例 3 件・壊れ 4 件 |
| G-16 | 希語本文が**実際に何の書体で描かれたか**を測る。一節点が単一の書体で描き切られていること。書体は `public/fonts/` に同梱し license も配る | CDP `CSS.getPlatformFontsForNode` + 和訳欄を陽性対照 / T-714〜T-716 |

### G-14(文体の指紋)の実測(2026-09-02)

予測を 4 つ先に書いて `loop_007` の `loop_start` に記録し、それから測った。
**通説の後期群(ソピステス・政治家・ピレボス・ティマイオス・クリティアス・法律)は
当てはめに一度も使っていない。** 使ったのは測り終えたあとの照合だけである。

| 予測 | 結果 | 実測 |
|---|---|---|
| 1. 後期群はヒアトゥス率が他より明確に低い | **成立** | 後期 0.170 / その他 0.337 |
| 2. ヒアトゥス率は篇の長さと相関しない | **成立** | r = −0.193 |
| 3. 後期 6 篇の分離は無作為 6 篇より有意 | **成立** | 置換検定 p = 0.00005(20,000 回・seed 20260902) |
| 4. 対話でない篇は後期群ほど下がらない | **成立** | 書簡集 0.228 / 弁明 0.392 |

後期 6 篇は 36 篇中の **1・3・4・5・6・7 位**を占める。2 位は通説外の『エピノミス』。

第二の軸(καθάπερ の取り分)は効果量 −2.31 でヒアトゥス率(2.08)より強い。
二軸の相関は −0.896 だが、**後期群を除いた 30 篇でも −0.767 残る** ——
群の分離が相関を作っているのではない。

**エリジョン率では説明できない**(効果量 −0.77・後期 26.8 / その他 23.3 回/千語)。
避け方はエリジョンではなく別のところにある。

ヒアトゥスの数え方は自前である(文の切れ目をまたがない・分母は母音で終わった組だけ)。
**公刊値と直接くらべることはしない。** くらべるのは同じ物差しで測った篇どうしに限る。

### G-13(語の照合)の実測(2026-09-02)

屈折語なので語幹の前方一致は**両方向に壊れる**。

- **拾いすぎ**: 語幹 `ειδ` は εἰδέναι(244)・εἰδώς(87)・εἴδωλον(26)を拾い、
  目当ての εἶδος(411)より多い 631 件が別語だった
- **取りこぼし**: 語幹 `πολι` は πόλις の斜格 πόλεως(281)・πόλει(391)を拾わない
- **畳みが区別を消す**: νόμος(法)と νομός(牧草地)、ἀγαθῶν と Ἀγάθων(人名)、
  ἐπιστημῶν と ἐπιστήμων(形容詞)は、アクセントを外すと同じ形になる

そこで語形を明示列挙し、表層で別語を落とす。判断は
`data/curated/word-ledger.json`(人が書いた台帳)に全部書く。
コードは台帳を当てるだけで、語形を推測しない。

### G-11 / G-12 の実測(2026-09-02、較正と検証を分けた)

上演型 27 篇を交互に割り、**14 篇で閾値を決め、13 篇で報告した**
(合わせた標本で検証しても何も分からない — HC-073)。

| 測るもの | 手がかり | 文末素通し | 語数だけ | 偶然 |
|---|---|---|---|---|
| 境界の**位置**(F1) | **0.567** | 0.569 | — | 0.043 |
| 節ごとの**濃さ**(相関 r) | **0.756** | 0.713 | −0.021 | — |

- **境界の位置は当てられない。** 手がかりの点数付けは文末素通しに負けた(勝ちは 5/13 篇)。
  偶然の 13 倍という数字は、**文の切れ目が効いているだけ**だった。この目玉は棄却する。
- **節ごとの濃さは当てられる。** 語数の交絡には **13/13 篇**で勝ち、
  素通しに対しても平均で上回る。効くのは「文がすべて交替ではない篇」で、
  メネクセノス 0.850 対 0.599、ラケス 0.855 対 0.731、法律 0.837 対 0.741。
  逆に応酬が密な篇(ヒッパルコス・ヒッピアス小)では素通しの方がわずかに良い。

**したがって画面②が出すのは「濃さ」であって「交替の位置」ではない。**

語り直し型の人称の交替は、**予測どおりには成立しなかった**(6/14 篇で p<0.01)。
成立したのは『国家』『カルミデス』『リュシス』『エウテュデモス』『プロタゴラス』『恋がたき』
—— いずれも**語り手自身が対話の参加者**である篇。『パイドン』『饗宴』『パルメニデス』は
語り手が伝聞するので発話動詞が 3 人称一色になり(パイドンは 329 件中 1 人称 11 件)、
この検定は原理的に使えない。**予測を書き換えずに、外れとして残す。**

### G-03 の非循環性について

自前検出器を Perseus の `<said>` で「較正」してはならない。較正すると
「Perseus のマークアップ方針を再現する検出器」ができるだけで、**マークアップが薄い篇では
同じように薄くなる**。検出器は本文の言語的手がかり(呼格・応答定型句・話者交替の語法)だけで
書き、`<said>` は**書き終えたあとの答え合わせにのみ**使う。
一致率は篇ごとに出し、一致しない篇は「どちらが正しいか」を本文で確かめる。

## 6. 権利

Perseus のデータは **CC BY-SA 4.0**。ここは曖昧にしない。

- 派生データ(正規化 JSON・計測値)と**和訳**は BY-SA 4.0 で公開する(訳は原文の二次的著作物)
- コードは MIT(`LICENSE` は分離して置く)
- Burnet 校訂本文(1900–07)・Loeb 英訳(1914–)は原著自体もパブリックドメインだが、
  Perseus 版としての BY-SA は継承する
- 各画面に出典と底本を明示する(N-04)

## 7. スコープ外

- プラトン以外の著者(偽プラトン文書を含む TLG 0059 の範囲のみを扱う)
- 写本異同・校訂註(apparatus criticus)の再現
- 全 574,506 語の完訳(第3層は篇単位で積み増す。全訳は目標にしない)
- 年代論の**確定**。F-07 は測定を示すだけで、成立順を主張しない
- LLM による解釈文を「データ」として提示すること。和訳と解題は人手(エージェント)の
  著作物として区別して表示する
