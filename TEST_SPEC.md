# TEST_SPEC.md — mondo-atlas

<!-- scaffold template v1.24.0 から展開(2026-09-02) -->

## 実行規約

- `pytest -x -q` を stage 3–5 の判定に使用。マーカー: `unit` / `integration` / `validation`
- フィクスチャ更新は専用コミット(`test: update fixtures`)で行い、理由をループログに記す
- 解析解を期待する合成フィクスチャは、期待値の導出前提(直交性・一意性・単一帰属等)を
  **テスト内の assert で検算**し、導出過程をコメントに残す。前提を検算しない期待値は
  正しい実装を落とす(VERIF-FALSE / HC-004)

## 期待値の出所(HC-016)

合成フィクスチャだけでなく、**外部データ・外部権威に対する期待値**にも同じ規律が要る。
ケースごとに、その期待値がどこから来たのかを書くこと。

| 出所 | 書き方 |
|---|---|
| SPEC の条項 | 条項 ID を書く。**SPEC の保証粒度を超える期待値を書かない**(超えるなら先に SPEC を上げる) |
| 外部権威(公表値・規格・検証データ) | 出典 URL と取得日をフィクスチャの先頭に書く |
| 実測 | **実測日と実測値**をコメントに残す。「誰かの仮定」と区別できるようにする |

件数・行数は定数で書かず、**集合の一致・取りこぼしの不在**という不変量で書く。
外部データは重複や欠落を含むため、数はデータが動くたびに壊れる。

## オラクルの出所

| フィクスチャ | 出所 | 性格 |
|---|---|---|
| `raw/perseus/**`(72 ファイル) | Perseus `canonical-greekLit` コミット `a1849c8`(2026-09-02 取得) | 外部権威・凍結済み |
| ステファヌス節 ID | TEI の `<milestone unit="section" resp="Stephanus">` | 外部権威(校訂者の付けた位置) |
| 希英の節対応 | 希側と英側の**独立した** milestone 集合 | 二経路一致。片方から他方を作らない |
| 本文トークン列 | 正規化前の TEI から直接取り出した列 | 自己照合(取りこぼし検出専用) |
| 上演型 29 篇の話者交替 | `<label>`(印刷面の話者記号) | **L2 の検出器の答え合わせ専用**。検出器の入力にしない(G-03) |

### G-03(循環の禁止)についての注記

`<said>` と `<label>` は L2 で書く発話交替検出器の**入力にしてはならない**。
入力にすると「Perseus の校訂方針を再現する検出器」ができ、
マークアップが薄い 7 篇では同じように薄くなる —— 緑のまま何も検出できない。
L0 では、正規化データが本文と話者記号を**別フィールドに分離して保持**することを検査する
(T-007)。これにより L2 で「本文だけを入力にする」ことがコードで担保できる。

## ケース一覧

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-001 | F-01 / G-01 | 節 ID が全集で一意 | 重複 0 件 |
| T-002 | F-01 / G-01 | 節 ID が篇内でステファヌス順に単調増加 | 全篇で成立 |
| T-003 | F-01 | 節 ID の形式が `<略号>.<頁><字>` | 全節が正規表現に一致 |
| T-004 | G-04 | 本文トークンの取りこぼしが無い(正規化前後で本文語の列が一致) | 全 36 篇で一致 |
| T-005 | G-02 | 希語本文にキリル文字・制御文字が無い | 違反 0 件 |
| T-006 | G-02 | 希語本文が NFC 正規化されている | 全篇で成立 |
| T-007 | F-01 / G-03 | 話者記号(`ΣΩ.`)が本文フィールドに混入しない | 本文に `label` 由来の文字列が無い |
| T-008 | G-05 | 希英の節 ID 集合が一致する(不一致は欠落として明示) | 欠落が記録されている |
| T-009 | F-03 | 全 36 篇にメタデータ(和題・略号・真贋区分)が揃う | 欠落 0 件 |
| T-010 | F-03 | 略号が全集で一意 | 重複 0 件 |
| T-011 | G-02 | **陽性対照**: キリル文字を混ぜた入力を検査器が落とす | 検出される |
| T-012 | G-04 | **陽性対照**: 本文を 1 語落とした正規化結果を検査が落とす | 検出される |
| T-013 | F-01 | 節に属する本文が、次の milestone までの範囲である(境界の帰属) | 合成 TEI で成立 |
| T-014 | F-03 | 真贋区分の値が定義済みの語彙のみ | 未定義値 0 件 |

### 陽性対照を対で置く理由(HC-041)

T-005 / T-004 は「違反 0 件」を主張する検出系である。**対象に違反が無いときと、
検査そのものが壊れているときとで、同じ緑を返す。** T-011 / T-012 はそれぞれの
検査器自身をテストし、緩めたときに気づけるようにする。

## 発話交替の検出 — `tests/test_turns.py`

| ID | 対応 | ケース | 期待 |
|---|---|---|---|
| T-201 | F-05a | アクセントを畳んで照合する(鋭/重の異形) | `ὅς` と `ὃς` が同一視される |
| T-202 | F-05a | 語境界を置く | `ἔφη` が `ἔφην` の内側に当たらない |
| T-203 | **G-03** | **話者記号を差し替えても出力が変わらない**(振る舞い) | 完全一致 |
| T-203b | G-03 | 検出器のコードが校訂者の欄名を持たない(ソース) | 参照なし |
| T-204 | F-05a | 境界は整列・重複なし・範囲内 | 成立 |
| T-205 | F-05a | 境界は文の途中に来ない | 成立 |
| T-206 | F-05a | **陽性対照**: 定型応答・問いへの短い答えを境界と見る | 検出 |
| T-207 | F-05a | **陰性対照**(実データの地の文): 境界を立てない | 0 件 |
| T-208 | F-05a | 発話動詞は人称を持つ | 3, 1 の順 |
| T-209 | F-05a | 発話動詞の位置が取れる | 整列・範囲内 |
| T-210 | G-11 | 適合率・再現率・F1 が定義どおり | 手計算と一致 |
| T-211 | G-11 | 空集合で 0 除算しない | 定義どおり |
| T-212 | G-11 | 許容幅は引数で明示(既定 0) | 成立 |
| T-213 | G-11 | **対照**: 無作為の基準値が出せる | 0 以上 0.5 未満 |
| T-214 | G-12 | 人称の交替率が出せる | 1.0 / 0.0 / 未定義 |
| T-215 | G-12 | **対照**: 置換検定が seed で再現する | 同値 |
| T-216 | **G-03** | **出荷物**に校訂者の印が載っていない | 欄も文字列も無い |
| T-217 | G-11 | 節ごとの交替数が**語数の交絡**に勝つ | r > 0.5 かつ語数より 0.3 以上高い |

### 循環の禁止を三方向で見る理由

G-03 は**振る舞い**(T-203)が主で、ソース検査(T-203b)と出荷物検査(T-216)は補助である。

- ソース検査だけでは、**言及と使用を区別できない**(説明の文で名前を挙げるのは違反ではない)
- 振る舞い検査だけでは、**たまたま影響が出なかった入力**を通す
- 出荷物検査が無いと、あとから誰かが「答えがそこにある」と使い、**静かに循環が復活する**

### 較正の結果は SPEC に置く

閾値や相関の**実測値**は SPEC の G-11 / G-12 節にある。テストには
「対照に勝つ」という不変量だけを書き、実測値そのものは定数で持たない
—— データが動けば数は動くが、不変量は動かない(HC-016)。

## 画面①(全集の俯瞰)— `tests-js/`

`vitest run` で実行する。実データ(`data/index.json`)に対する検査を含む。

| ID | 対応要求 | ケース | 期待 |
|---|---|---|---|
| T-101 | F-03 | 36 篇が揃い、四部作は 9 群 × 4 篇 | 成立 |
| T-102 | F-04 | 図が使う数値がすべて有限の非負 | 全篇で成立 |
| T-103 | F-03 | 真贋区分は定義済みの 3 種のみ | 未定義値 0 |
| T-104 | F-04 | 四部作順は tlg 番号順で、群が連続する | 成立 |
| T-105 | F-04 | 長い順は降順で、同値でも安定 | 成立(同値の実在を先に確認) |
| T-106 | F-04 | 並べ替えで作品が増減しない | 36 篇 |
| T-107 | G-07 | 目盛りは 0 始まり・単調増加・最大値を覆う | 成立 |
| T-108 | G-07 | 刻みは 1・2・5 × 10^n | 成立 |
| T-109 | F-04 | 帯は 0〜100% に収まる | 成立 |
| T-110 | G-07 | 軸上限 = 目盛りの最後(格子とラベルの出所がひとつ) | 一致 |
| T-111 | G-07 | 凡例は図に出ている作品から数える | 部分集合で追随する |

### フッタ判定規則 — `harness/footer-rule.mjs`

**規則は検品器の中に書かない**(HC-080)。壊し方ごとの陽性対照つきで単体検査する。

| ID | ケース | 期待 |
|---|---|---|
| T-120 | 規約どおりのフッタ(**陰性対照**) | 違反 0 件 |
| T-121 | 実装の `links.ts` が規約の並びを満たす | 5 項目・この順 |
| T-122 | 項目が欠けたら落ちる | 検出 |
| T-123 | 並びが入れ替わったら落ちる | 検出 |
| T-124 | 区切りが `innerText` から消えたら落ちる | 検出 |
| T-125 | 著作権表示が無い / リンク文言の中にある | 検出 |
| T-126 | MIT License の行き先が LICENSE 以外 | 検出 |
| T-127 | GitHub の行き先が別ホストに化けた | 検出(HC-098) |
| T-128 | `position: fixed` / `bottom: 0` でない | 検出 |

## 画面②(問答の呼吸)— `tests-js/breath.test.ts`

| ID | 対応 | ケース | 期待 |
|---|---|---|---|
| T-301 | F-05 | 36 篇あり、並列配列の長さが揃う | 成立 |
| T-302 | **G-03** | 配られるデータに校訂者の印が無い | 文字列として不在 |
| T-303 | F-01 | 節の合計が全集の実測と一致 | 8,559 |
| T-304 | G-01 | 頁は篇内で非減少(書簡集を除く) | 成立 |
| T-305 | F-05 | 交替数・語数は非負の有限値 | 成立 |
| T-306 | F-05c | 検証の状態は定義済み語彙で、照合済みが過半 | 成立 |
| T-307 | F-05 | 濃さは千語あたり、語数 0 の節は 0 | 成立 |
| T-308 | G-07 | 移動平均は長さを変えず端を欠測にしない | 成立 |
| T-309 | G-07 | 面の経路は閉じ、値が軸を越えない | 0 ≤ y ≤ H |
| T-310 | G-07 | 巻の切れ目は同じ配列から導く | 国家 10・法律 12・他 0 |
| T-311 | F-05b | 共通の縦軸上限をどの篇も越えない | 成立 |
| T-312 | G-07 | 要約の数は図と同じデータから導く | 一致 |
| T-313 | F-05c | 検証の状態を三段に言い分ける | high/mid/low |
| T-314 | F-05 | **実測した篇内の形が保たれている** | メネクセノスの両端 > 中央 |

T-314 は「図が示すはずの話が、まだデータに在るか」を固定する。
実測(2026-09-02)で見つけた形 —— メネクセノスは葬送演説の間だけ平坦 —— を
不変量として書いてあるので、推定器を触って形が壊れれば落ちる。

## 実ブラウザ検品 — `harness/smoke.mjs`

目で見なければ分からない性質は、テストで代替せず**検品の手順**として持つ(HC-041)。
390 / 768 / 1280 px の 3 幅で実際に描かせ、次を測る。

- 横の溢れ / 縦の伸びすぎ(16,000px 超は表を疑う)
- **固定フッタの高さ < body の逃げ**(幅ごとに。390px では 67px まで育つ)
- フッタ規約(`footer-rule.mjs` に委譲)
- 帯の本数・四部作見出しの数が実データと一致すること
- **目盛りの本数と格子の本数が一致すること**(出所がひとつである証拠)
- **ラベルが切れていないこと / 帯が帯域を越えていないこと**

最後の 2 項目は、**目視で見つけた故障を機械で捕まえ直したもの**である
(最長の篇で値ラベルが右端に溢れて切れていたが、横溢れの検査は緑のまま通した)。

検品器自身にも陽性対照を置く(`--self-test`)。正常な入力 1 件を通し、
壊れた入力 9 件を落とすことを毎回確かめてから走査に入る。
終了コードは 合格 0 / 検品の不合格 1 / **検品器自身の異常 2**。
